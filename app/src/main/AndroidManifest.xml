<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teacherassistantapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UpdateAttendance"></activity>
        <activity android:name=".StatisticsActivity" />
        <activity android:name=".TakeAttendActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".AddClassActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>